---
http:
  middlewares:
    security:
      chain:
        middlewares:
          - https-only@file

    https-only:
      chain:
        middlewares:
          - https-redirect@file
          - hsts@file